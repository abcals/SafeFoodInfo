<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SafeFoodInfo.mapper.BoardMapper">
    <insert id="insertBoardContent">
        INSERT INTO board_list
        (bl_title, bl_content, bl_wuser, bl_file)
        VALUES
        (#{bl_title}, #{bl_content}, #{bl_wuser}, #{bl_file});
    </insert>
    <select id = "selectBoardList" resultType = "SafeFoodInfo.vo.board_vo.BoardListVO">
        SELECT bl_seq, bl_title, bl_wuser, bl_wdate, bl_mdate, bl_cnt
        FROM board_list;
    </select>
</mapper>